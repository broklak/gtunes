import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
  },
  loaderContainer: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center'
  },
  textLoader: {
    fontSize: 28,
  },
  video: {
    position: 'absolute',
    top: 0,
    left: 0,
    bottom: 0,
    right: 0,
  }
});