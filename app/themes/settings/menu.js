import CustomMenu from '../../src/components/customMenuComponent';

export const menuOptions = {
	drawerWidth: 250,
	contentComponent: CustomMenu,
	contentOptions: {
		activeTintColor: 'white',
		inactiveTintColor: 'grey'
	}
}